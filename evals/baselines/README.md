# Eval baselines

Named snapshots of L1 scorecard pass/fail per golden id.
Created by `npm run eval:baseline -- --tag <name>`.

Compare with:
`npx tsx evals/runner.ts --llm-mode off --baseline <name>`
