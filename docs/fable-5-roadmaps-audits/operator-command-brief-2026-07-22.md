# Operator command brief — where you are and what happens next

**Date:** 2026-07-22  
**Authority:** Operator (you). Agents implement and advise. Nothing is “binned” without your express decision.  
**Tone:** Straight map after a confusing Sol plan + token spend. No personality sanitization agenda.

---

## 1. What the ~$8 bought (not wasted if we execute)

You paid for a **high-reasoning plan** (GPT Sol plan mode), not for implementation.

| Artifact | Path | Status |
|----------|------|--------|
| Sol plan-of-record | `docs/fable-5-roadmaps-audits/wp-ops-revision-and-archive-plan-2026-07-22.md` | **ACK** (your answers C + A) |
| Paste prompt that created it | `…/prompt-sol-ops-revision-and-archive-plan-2026-07-21.md` | archive |
| Cursor copy of plan | `~/.cursor/plans/ops_revision_archival_07e388a0.plan.md` | same content |
| Master roadmap pointer | `master-roadmap-2026-07.md` row **OPS-REV + ARCH plan** | linked |
| **OPS-REV-0** (docs) | OOC v5 + regenerate runbook | **done** (Grok Build; free of Sol tokens) |

**If we never implement OPS-REV-1+, the money only bought a PDF-in-git.**  
**If we implement the plan as frozen WPs, that spend is the design tax and was the right order.**

Your frozen decisions inside the plan:

1. **ARCH-1:** browser download first; Guardian write to `backups/threads/` later.  
2. **Edited Benjamin regen:** may stage a real scene move, but **held + provenance-tagged, never auto-approve.**

---

## 2. What is live in the story (one paragraph)

- Track day / 6:54.2 / debrief / night drive: **history.**  
- Live disk `current-state.md` (unless you later approve a stage): **Schloss Lieser cabin, engine off, hands joined** (or whatever you last approved — check the file header if play moved).  
- There may be a **pending staged** suite rewrite — **not live until you approve.**  
- Affalterbach is **Monday** itinerary pressure, not “tonight.”  
- Feeling that Scarlett’s **personality** shifted after the track day is a **separate concern** from OPS-REV (systems). It is valid and should be checked at the hotel beat with a focused audit, not hand-waved as “PC/ToS.”

---

## 3. Who does what (how to use the team)

| Seat | Best use | Bad use |
|------|----------|---------|
| **You (Operator)** | Freeze WPs, approve/reject stages, play IC, say “go / stop / park *with my OK*” | Expect agents to own product direction without you |
| **Grok Build (this seat)** | Implement frozen WPs, reindex, force-sync state, write runbooks, cheap sequential code | Vague “make it better” with no WP |
| **GPT Sol** | Plan mode for multi-file design; **one** implement WP when you want expensive thoroughness | Full-stack “build the plan” in one go (token burn) |
| **Fable / architecture** | Roadmap shape, design docs, sequencing | Silent parking of your ideas |
| **Gemini** | Review plans/evidence, co-pilot play, catch continuity/personality drift in prose | Own the repo as sole implementer |
| **Guardian LLM** | Per-turn continuity gate (LIVE BEAT, duplex, block bad prose) | Not a product manager; not your GM offline brain yet |
| **RAG** | Memory retrieval / index | Not “personality” |

**Rule of thumb:**  
- **Confused about direction** → short session with Grok Build or Gemini **ideas**, write decisions to disk.  
- **Multi-surface design** → Sol **plan only**, then stop.  
- **Build one WP** → Grok Build by default; Sol only if you want that model on code and accept cost.  
- **Play** → Guardian + RAG + your OOC v5; Gemini optional co-pilot.

---

## 4. Ideas are not binned without you

Earlier language (“parked”, “deferred”, “does not block…”) meant **not scheduled as the next code ticket**, not **deleted** or **forbidden**. That distinction was poorly communicated. **Your project: nothing is closed unless you say so.**

### Active backlog you still own (re-opened explicitly)

| Idea | Status | Feasibility (honest) |
|------|--------|----------------------|
| **OPS-REV + ARCH plan** | ACK; REV-0 done; code not started | **Feasible.** Best next engineering. |
| **Living GM / campaign GM** | **Stays where Operator left it — do not re-open or “unpark” without Operator asking.** Design file may exist; not on the active work list (Operator 2026-07-22: do not unpark). |
| **Truman Show / environment director** | Only if Operator brings it up | — |
| **Agency Observatory / character-first 6.5** | On master roadmap as pending | Only when Operator prioritizes. |
| **Thread archival (ARCH)** | In OPS plan (1A download → 1B server) | Feasible; 1A is high value after Thread 7 scare. |
| **Multi-beat LIVE BEAT catch-up (OPS-REV-5)** | In plan, later | Feasible; harder; manual rewrite works today. |
| **Anti-parroting / personhood spark** | Discussed; no single WP frozen | Feasible as play discipline + goldens + optional auditor; not “more agents = fixed.” |
| **Affalterbach 5.10 ensemble stress** | Roadmap pending | Feasible when story reaches HQ; **does not block** OPS-REV code. |

If something is **not feasible**, agents must **say so in writing** with why, and leave the decision to you.

---

## 5. What does *not* wait on AMG HQ

Almost everything. HQ is a **story + ensemble burn-in** (5.10), not a padlock on engineering.

**Can run in parallel with hotel/Autobahn play:** OPS-REV-1→4, ARCH-1A, personality audit, GM **ideas** (docs only until freeze).

**Better timed with/after HQ play:** 5.10 live ensemble checks; optionally REV-5 if multi-scene lag bites again.

---

## 6. Suggested order (A1 quality, low thrash)

Do **one track at a time** so money and attention don’t scatter.

### Track A — Spend the plan (systems reliability)  ← recommended first for the $8

1. ~~OPS-REV-0 docs~~ **done**  
2. **OPS-REV-1** — turn contract + no-write / held-stage (**first code**)  
3. OPS-REV-2 bridge finality  
4. OPS-REV-3 revision-aware auditor  
5. OPS-REV-4 goldens/telemetry  
6. ARCH-1A one-click export when you want archival next  

Say: **`go OPS-REV-1`** to the implementer you choose (Grok Build recommended for cost).

### Track B — Personality / “did she change after track day”

- At hotel: short Gemini or Grok **cold read** of last 5–10 IC turns vs `benjamin-character-bible` / Scarlett autonomy lines / pre-track-day voice.  
- Output: concrete drift list (agency, Swedish, dominance, technical confidence, softness) — **not** ToS cleanup.  
- Only then: optional goldens or instruction nits.  

### Track C — Ideas / next phase (includes GM)

- Dedicated session: **ideas only**, write to disk.  
- Living GM back on the table: feasibility already exists; you decide keep / reshape / delay.  
- Output: prioritized backlog **you** signed, not agent parking.

### Track D — Play

- Use `docs/operator-how-to-regenerate-safely.md` + OOC v5.  
- Approve/reject staged suite only if the suite is true in play.

---

## 7. Workflow we should use from now (proposal)

1. **You state goal in one sentence** (or “I can’t articulate — help me.”).  
2. **Agent restates** goal + non-goals in 5 bullets; you correct.  
3. **Classify:** play | ops hygiene | implement WP | ideas | review.  
4. **If implement:** name WP from plan; one WP; evidence doc; build/test.  
5. **If ideas:** no code; write parking/ACK file; **you** mark keep/delay/kill.  
6. **If Sol:** plan mode only unless you explicitly pay for implement.  
7. **Long prompts:** always **write to disk** (Grok CLI select/scroll is bad).  
8. **Never** “park” an Operator-owned idea without a line: *“Operator decision needed: keep / delay / kill.”*

---

## 8. What to do in the next 15 minutes

1. Open this file + `wp-ops-revision-and-archive-plan-2026-07-22.md` (editor, not TUI select).  
2. Skim executive summary + WP list only.  
3. Pick **one** reply to the implementer:

```text
A) go OPS-REV-1 — implement the first code WP from the paid plan, A1 quality, evidence doc
B) ideas session first — Living GM + next phase, write decisions to disk, no code
C) personality audit — hotel-ready checklist vs pre-track voice
D) play only — just confirm staged/current-state and stay out of code
E) workflow only — refine the multi-agent workflow doc, no product code
```

You can stack later (A then B). Do not stack A+B+C in one Sol burn.

---

## 9. Apology boundary (agents)

Cost and confusion after a paid plan without a crisp “you are here / push this button next” is a **process failure**. The plan content is good; the **handoff** was not. This brief exists so that does not repeat.

**Your project. Your freeze. Your go.**
